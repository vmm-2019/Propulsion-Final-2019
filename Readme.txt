This directory contains scripts (python, sql) used in the project.

Currently scripts are stored out of the package context in which they are declared. This is due to problems related with the commitment of directories